--// AutoObby - VIP FAST PERFECT (NO PUBLIC, 3x FASTER)
--// Smart 2s delay + 2s checks → 100% VIP lock
--// Auto-restart + Guardian → UNKILLABLE

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local TeleportService = game:GetService("TeleportService")
local VirtualUser = game:GetService("VirtualUser")

local PLACE_ID = 11276071411
local VIP_CODE = "d610a52456bfe5468e5b0e36bde089c2"  -- ← YOUR CODE
local SCRIPT_URL = "https://raw.githubusercontent.com/RobloxScriptDuH/sac/refs/heads/main/Scripty"

--// LocalPlayer
local plr = Players.LocalPlayer or Players.PlayerAdded:Wait()

--// Anti-AFK (every 30s)
task.spawn(function()
    while task.wait(30) do
        pcall(VirtualUser.ClickButton2, VirtualUser, Vector2.new())
    end
end)

--// Wait for Lobby (fast)
local lobby
repeat task.wait(0.5) until Workspace:FindFirstChild("Lobby")
lobby = Workspace.Lobby

--// QUEUE ON TELEPORT
local queueTeleport = syn and syn.queue_on_teleport or getgenv().queue_on_teleport or fluxus and fluxus.queue_on_teleport
if queueTeleport then
    plr.OnTeleport:Connect(function()
        pcall(queueTeleport, "loadstring(game:HttpGet('" .. SCRIPT_URL .. "'))()")
    end)
end

--// FORCE VIP (FAST & SAFE)
local function forceVIP()
    if queueTeleport then
        pcall(queueTeleport, "loadstring(game:HttpGet('" .. SCRIPT_URL .. "'))()")
    end

    print("VIP: REJOINING PRIVATE...")
    task.wait(2)  -- Critical delay to avoid public
    pcall(function()
        TeleportService:Teleport(PLACE_ID, plr, nil, nil, {privateServerLinkCode = VIP_CODE})
    end)
end

--// VIP GUARDIAN (Every 2s — FAST & RELIABLE)
task.spawn(function()
    task.wait(3)

    while true do
        task.wait(2)
        pcall(function()
            local url = game:GetService("ContentProvider").BaseUrl
            if url and not string.find(url, VIP_CODE) then
                print("VIP: IN PUBLIC → FORCING PRIVATE")
                forceVIP()
            else
                print("VIP: LOCKED")
            end
        end)
    end
end)

--// SCRIPT HEARTBEAT + AUTO-RESTART
local lastHeartbeat = tick()
task.spawn(function()
    while true do
        lastHeartbeat = tick()
        task.wait(1)
    end
end)

task.spawn(function()
    while true do
        task.wait(5)
        if tick() - lastHeartbeat > 10 then
            print("VIP: SCRIPT DIED → RESTART")
            pcall(loadstring, game:HttpGet(SCRIPT_URL))
            break
        end
    end
end)

--// OBBY (FAST & SAFE)
task.spawn(function()
    task.wait(1)
    local char = plr.Character or plr.CharacterAdded:Wait()
    local hrp = char:WaitForChild("HumanoidRootPart")

    local obby = lobby:FindFirstChild("Obby")
    if not obby then return end

    local function touch(part)
        if not part then return end
        task.wait(0.2)
        pcall(function()
            hrp.CFrame = part.CFrame + Vector3.new(0, 3, 0)
            if firetouchinterest then
                firetouchinterest(hrp, part, 0)
                task.wait(0.05)
                firetouchinterest(hrp, part, 1)
            end
        end)
    end

    touch(obby:FindFirstChild("ObbyEndPart"))
    touch(obby:FindFirstChild("HardObbyEndPart"))

    print("Obby done → VIP ACTIVE")
end)

print("AutoObby: VIP FAST PERFECT LOADED - NO PUBLIC")
